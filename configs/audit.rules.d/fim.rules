## Monitor modifications and attribute changes to /etc/shadow (standard FIM)
-w /etc/shadow -p wa -k shadow_changes

## Monitor reads (credential dumping) + modifications to /etc/shadow
-w /etc/shadow -p rwa -k shadow_access

## Similarly for /etc/sudoers
-w /etc/sudoers -p wa -k sudoers_changes
-w /etc/sudoers -p rwa -k sudoers_access

## Also monitor /etc/sudoers.d/ directory (common drop location)
-w /etc/sudoers.d/ -p wa -k sudoers_d_changes

## Broader integrity (CIS/PCI best practices)
-w /etc/passwd -p wa -k passwd_changes
-w /etc/group -p wa -k group_changes